nelmio_api_doc:
    documentation:
        info:
            title: GeoTensions API
            description: REST API serving filtered ACLED data as GeoJSON.
            version: 0.3.0
        components:
            securitySchemes:
                bearerAuth:
                    type: http
                    scheme: bearer
    areas:
        path_patterns:
            - ^/api(?!/doc$)
